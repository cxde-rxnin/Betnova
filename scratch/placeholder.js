require("dotenv").config({ path: ".env" });
const { SportApi7Provider } = require("./features/sportsbook/providers/sportapi7.ts");

// This won't work perfectly because it's TypeScript, so I will write a simple fetch script instead.
